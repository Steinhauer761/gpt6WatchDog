# Onion discovery privacy behavior

The discovery step searches public indexes and public posts. It does not open the discovered onion service and does not submit forms, log in, download files, transact, or attempt to identify operators or visitors.

Direct onion retrieval remains a separate explicit action through the bounded Tor crawler. That action requires the backend Tor SOCKS proxy to be configured.
