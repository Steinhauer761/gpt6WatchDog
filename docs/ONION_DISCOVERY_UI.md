# Onion discovery console controls

The Next.js research console includes a dedicated discovery panel with an optional topic field and category selector. A discovery request can return up to 100 unique onion addresses per run, ranked by independent public-source mentions. Each result exposes provenance and can be copied into the existing bounded onion crawler.
